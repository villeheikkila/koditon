{# Escape '$' to avoid Compose variable interpolation inside secret values. #}
{% for key, value in infra_env | dictsort %}
{{ key }}={{ ((value | string) | replace('$', '$$')) | to_json }}
{% endfor %}
BACKEND_IMAGE={{ backend_image }}
