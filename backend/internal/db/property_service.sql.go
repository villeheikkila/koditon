// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: property_service.sql

package db

import (
	"context"
	"encoding/json"

	"github.com/google/uuid"
)

const getSaleListingSourceMediaData = `-- name: GetSaleListingSourceMediaData :one
SELECT
    evidence.provider AS sale_listing_source_provider,
    CASE
        WHEN evidence.source_kind = 'frontdoor_building_announcement' THEN 'announcement'
        WHEN evidence.source_kind = 'frontdoor_ad' THEN 'ad'
        WHEN evidence.source_kind = 'shortcut_ad' THEN 'ad'
        ELSE evidence.source_kind
    END AS sale_listing_source_kind,
    COALESCE(sa.shortcut_ad_data, '{}'::jsonb) AS shortcut_ad_data,
    COALESCE(fa.frontdoor_ad_data, '{}'::jsonb) AS frontdoor_ad_data,
    fba.frontdoor_building_announcement_main_image_uri AS frontdoor_building_announcement_main_image_uri
FROM public.listing_search_documents doc
JOIN public.evidence_sources evidence ON evidence.evidence_source_id = doc.primary_evidence_source_id
LEFT JOIN origin.shortcut_ads sa ON sa.shortcut_ad_id = evidence.shortcut_ad_id
LEFT JOIN origin.frontdoor_ads fa ON fa.frontdoor_ad_id = evidence.frontdoor_ad_id
LEFT JOIN origin.frontdoor_building_announcements fba ON fba.frontdoor_building_announcement_id = evidence.frontdoor_building_announcement_id
WHERE doc.primary_source_listing_id = $1
    AND doc.listing_status = 'active'
ORDER BY doc.last_seen_at DESC NULLS LAST, doc.refreshed_at DESC
LIMIT 1
`

type GetSaleListingSourceMediaDataRow struct {
	SaleListingSourceProvider                 *string         `json:"sale_listing_source_provider"`
	SaleListingSourceKind                     *string         `json:"sale_listing_source_kind"`
	ShortcutAdData                            json.RawMessage `json:"shortcut_ad_data"`
	FrontdoorAdData                           json.RawMessage `json:"frontdoor_ad_data"`
	FrontdoorBuildingAnnouncementMainImageUri *string         `json:"frontdoor_building_announcement_main_image_uri"`
}

func (q *Queries) GetSaleListingSourceMediaData(ctx context.Context, dollar_1 *uuid.UUID) (GetSaleListingSourceMediaDataRow, error) {
	row := q.db.QueryRow(ctx, getSaleListingSourceMediaData, dollar_1)
	var i GetSaleListingSourceMediaDataRow
	err := row.Scan(
		&i.SaleListingSourceProvider,
		&i.SaleListingSourceKind,
		&i.ShortcutAdData,
		&i.FrontdoorAdData,
		&i.FrontdoorBuildingAnnouncementMainImageUri,
	)
	return i, err
}

const listBuildingOfferingSourceListingIDs = `-- name: ListBuildingOfferingSourceListingIDs :many
SELECT
    doc.primary_source_listing_id AS sale_listing_id
FROM public.property_units pu
JOIN public.property_offerings po ON po.property_unit_id = pu.property_unit_id
JOIN public.listing_search_documents doc ON doc.property_offering_id = po.property_offering_id
WHERE pu.housing_company_id = $1
    AND doc.listing_status = 'active'
    AND doc.kind IN ('ad', 'announcement')
    AND doc.primary_source_listing_id IS NOT NULL
ORDER BY
    CASE WHEN doc.kind = 'ad' THEN 0 ELSE 1 END,
    doc.last_seen_at DESC NULLS LAST,
    doc.refreshed_at DESC
LIMIT 200
`

func (q *Queries) ListBuildingOfferingSourceListingIDs(ctx context.Context, dollar_1 *uuid.UUID) ([]*uuid.UUID, error) {
	rows, err := q.db.Query(ctx, listBuildingOfferingSourceListingIDs, dollar_1)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []*uuid.UUID{}
	for rows.Next() {
		var sale_listing_id *uuid.UUID
		if err := rows.Scan(&sale_listing_id); err != nil {
			return nil, err
		}
		items = append(items, sale_listing_id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listHousingCompanyRenovationEvents = `-- name: ListHousingCompanyRenovationEvents :many
SELECT
    event.category,
    COALESCE(event.year, event.start_year)::int4 AS year
FROM public.property_renovation_events event
WHERE event.target_type = 'housing_company'
    AND event.target_id = $1
    AND event.category <> ''
    AND event.status = 'done'
ORDER BY event.category, COALESCE(event.year, event.start_year) NULLS LAST
`

type ListHousingCompanyRenovationEventsRow struct {
	Category string `json:"category"`
	Year     *int32 `json:"year"`
}

func (q *Queries) ListHousingCompanyRenovationEvents(ctx context.Context, dollar_1 *uuid.UUID) ([]ListHousingCompanyRenovationEventsRow, error) {
	rows, err := q.db.Query(ctx, listHousingCompanyRenovationEvents, dollar_1)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListHousingCompanyRenovationEventsRow{}
	for rows.Next() {
		var i ListHousingCompanyRenovationEventsRow
		if err := rows.Scan(&i.Category, &i.Year); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listSaleListingFallbackRenovations = `-- name: ListSaleListingFallbackRenovations :many
SELECT
    source_listing_renovation_category,
    source_listing_renovation_status,
    source_listing_renovation_year,
    source_listing_renovation_component AS source_listing_renovation_component,
    source_listing_renovation_scope AS source_listing_renovation_scope,
    source_listing_renovation_stage AS source_listing_renovation_stage,
    source_listing_renovation_responsibility AS source_listing_renovation_responsibility,
    source_listing_renovation_cost_estimate_eur,
    source_listing_renovation_text AS source_listing_renovation_text,
    source_listing_renovation_confidence,
    source_listing_renovation_source_field
FROM public.source_listing_renovations
WHERE source_listing_id = $1
ORDER BY source_listing_renovation_category, source_listing_renovation_year NULLS LAST
`

type ListSaleListingFallbackRenovationsRow struct {
	SourceListingRenovationCategory        string  `json:"source_listing_renovation_category"`
	SourceListingRenovationStatus          string  `json:"source_listing_renovation_status"`
	SourceListingRenovationYear            *int32  `json:"source_listing_renovation_year"`
	SourceListingRenovationComponent       *string `json:"source_listing_renovation_component"`
	SourceListingRenovationScope           *string `json:"source_listing_renovation_scope"`
	SourceListingRenovationStage           *string `json:"source_listing_renovation_stage"`
	SourceListingRenovationResponsibility  *string `json:"source_listing_renovation_responsibility"`
	SourceListingRenovationCostEstimateEur *int64  `json:"source_listing_renovation_cost_estimate_eur"`
	SourceListingRenovationText            *string `json:"source_listing_renovation_text"`
	SourceListingRenovationConfidence      int32   `json:"source_listing_renovation_confidence"`
	SourceListingRenovationSourceField     string  `json:"source_listing_renovation_source_field"`
}

func (q *Queries) ListSaleListingFallbackRenovations(ctx context.Context, dollar_1 *uuid.UUID) ([]ListSaleListingFallbackRenovationsRow, error) {
	rows, err := q.db.Query(ctx, listSaleListingFallbackRenovations, dollar_1)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListSaleListingFallbackRenovationsRow{}
	for rows.Next() {
		var i ListSaleListingFallbackRenovationsRow
		if err := rows.Scan(
			&i.SourceListingRenovationCategory,
			&i.SourceListingRenovationStatus,
			&i.SourceListingRenovationYear,
			&i.SourceListingRenovationComponent,
			&i.SourceListingRenovationScope,
			&i.SourceListingRenovationStage,
			&i.SourceListingRenovationResponsibility,
			&i.SourceListingRenovationCostEstimateEur,
			&i.SourceListingRenovationText,
			&i.SourceListingRenovationConfidence,
			&i.SourceListingRenovationSourceField,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listSaleListingValuationClaimTargets = `-- name: ListSaleListingValuationClaimTargets :many
WITH linked AS (
    SELECT pu.property_unit_id, pu.physical_building_id, pu.housing_company_id
    FROM public.listing_search_documents doc
    JOIN public.property_offerings po ON po.property_offering_id = doc.property_offering_id
    JOIN public.property_units pu ON pu.property_unit_id = po.property_unit_id
    WHERE doc.primary_source_listing_id = $1
        AND doc.listing_status = 'active'
    ORDER BY doc.last_seen_at DESC NULLS LAST, doc.refreshed_at DESC
    LIMIT 1
)
SELECT 'sale_listing'::text AS entity_type, $1::uuid AS entity_id
UNION ALL SELECT 'property_unit'::text, property_unit_id FROM linked WHERE property_unit_id IS NOT NULL
UNION ALL SELECT 'physical_building'::text, physical_building_id FROM linked WHERE physical_building_id IS NOT NULL
UNION ALL SELECT 'housing_company'::text, housing_company_id FROM linked WHERE housing_company_id IS NOT NULL
`

type ListSaleListingValuationClaimTargetsRow struct {
	EntityType string    `json:"entity_type"`
	EntityID   uuid.UUID `json:"entity_id"`
}

func (q *Queries) ListSaleListingValuationClaimTargets(ctx context.Context, dollar_1 *uuid.UUID) ([]ListSaleListingValuationClaimTargetsRow, error) {
	rows, err := q.db.Query(ctx, listSaleListingValuationClaimTargets, dollar_1)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListSaleListingValuationClaimTargetsRow{}
	for rows.Next() {
		var i ListSaleListingValuationClaimTargetsRow
		if err := rows.Scan(&i.EntityType, &i.EntityID); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const resolveBuildingPublicID = `-- name: ResolveBuildingPublicID :one
SELECT physical_building_id::text AS canonical_id
FROM public.physical_buildings
WHERE ('b_' || substr(md5(physical_building_id::text), 1, 16)) = $1
LIMIT 1
`

func (q *Queries) ResolveBuildingPublicID(ctx context.Context, dollar_1 *string) (*string, error) {
	row := q.db.QueryRow(ctx, resolveBuildingPublicID, dollar_1)
	var canonical_id *string
	err := row.Scan(&canonical_id)
	return canonical_id, err
}

const resolveRentalPublicID = `-- name: ResolveRentalPublicID :one
SELECT doc.canonical_id
FROM public.listing_search_documents doc
WHERE doc.listing_type = 'rental'
    AND doc.listing_status = 'active'
    AND ('r_' || substr(md5(doc.canonical_id), 1, 16)) = $1
ORDER BY doc.last_seen_at DESC NULLS LAST
LIMIT 1
`

func (q *Queries) ResolveRentalPublicID(ctx context.Context, dollar_1 *string) (string, error) {
	row := q.db.QueryRow(ctx, resolveRentalPublicID, dollar_1)
	var canonical_id string
	err := row.Scan(&canonical_id)
	return canonical_id, err
}
